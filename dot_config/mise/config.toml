[tools]

# ================================================
#  CLI APPS - NOT AVAILABLE IN BREW
# ================================================

usql                                    = "latest"  # universal sql cli client (brew compiles with go and fails)
"github:wezterm/wezterm" = { version = "nightly", asset_pattern = "wezterm-nightly.Ubuntu26.04.tar.xz" }  # brew tap installs old AppImage even with HEAD
"github:Veirt/weathr"                   = "latest"  # terminal weather app
"github:Wraient/curd"                   = "latest"  # uwu
"github:olastor/age-plugin-fido2-hmac"  = "latest"  # use age with solokey
"github:Universal-Debloater-Alliance/universal-android-debloater-next-generation" = "latest"  # debloat android with computer

# ================================================
#  LANGUAGE RUNTIMES
# ================================================

clojure                                 = "latest"
go                                      = "latest"
java                                    = "latest"
node                                    = "latest"
python                                  = "latest"
ruby                                    = "latest"
rust                                    = "latest"
lua                                     = "latest"

# ================================================
#  LANGUAGE & FRAMEWORK SPECIFIC TOOLS
# ================================================

# python
uv                                      = "latest"  # fast python package manager (by Astral)
ruff                                    = "latest"  # fast python linter + formatter (by Astral)
ty                                      = "latest"  # extremely fast python type checker (by Astral)

# rust
"cargo:bacon"                           = "latest"  # background code checker
"cargo:loco"                            = "latest"  # rust web framework cli (Rails-like)
"cargo:sea-orm-cli"                     = "latest"  # seaorm migration cli

# clojure
babashka                                = "latest"  # clojure scripting (also in brew)
clj-kondo                               = "latest"  # linter for clojure code that sparks joy
"github:clojure-lsp/clojure-lsp"        = "latest"  # clojure language server
"github:weavejester/cljfmt"             = "latest"  # clojure code formatter
